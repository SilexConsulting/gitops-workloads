apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Indiscriminate workload resources — applied to every cluster labelled enable_resources=true.
# Only list manifests that are safe on any cluster (no operator/workload dependency).
# Empty is valid — it renders nothing.
resources: []
